{
  "name": "ticket-system",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "db:push": "pnpm -C apps/api prisma db push",
    "db:migrate": "pnpm -C apps/api prisma migrate dev",
    "seed": "tsx apps/api/prisma/seed.ts"
  },
  "devDependencies": {
    "tsx": "^4.16.0"
  }
}
